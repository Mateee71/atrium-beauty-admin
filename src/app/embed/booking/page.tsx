import BookingEmbed from "./bookingEmbed";

export default function BookingPage() {
  return (
    <main
      className="booking-embed-light min-h-screen bg-transparent p-0"
      style={{ colorScheme: "light" }}
    >
      <BookingEmbed />
    </main>
  );
}